# direnv configuration for netcup-kube
# Run `direnv allow` to activate

# Add bin/ to PATH
PATH_add bin

# Set KUBECONFIG to local k3s config (if exists)
if [[ -f "./config/k3s.yaml" ]]; then
  export KUBECONFIG="$(expand_path ./config/k3s.yaml)"
fi

# Optional: Load environment from config/netcup-kube.env (if exists)
if [[ -f "./config/netcup-kube.env" ]]; then
  dotenv ./config/netcup-kube.env
fi
