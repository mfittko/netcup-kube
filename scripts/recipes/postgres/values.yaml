auth:
  # postgresPassword will be set via --set if provided
  database: app
  username: app

primary:
  persistence:
    enabled: true
    size: 8Gi

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    labels:
      release: kube-prometheus-stack
