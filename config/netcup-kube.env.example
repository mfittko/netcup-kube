# Copy to config/netcup-kube.env and adjust as needed.
#
# This is the local inventory/config format for netcup-kube helper scripts.
# It stays intentionally simple and dependency-free.
#
# NOTE: This file is bash-sourced by local helper scripts (e.g. bin/netcup-kube-tunnel),
# so keep it simple: KEY=value (no spaces around '='), avoid command substitution, etc.

# Default SSH user (used if MGMT_USER is empty and by other helper scripts)
DEFAULT_USER=ops

# Management node (set either host or ip)
MGMT_HOST=mfittko.com
MGMT_IP=
MGMT_USER=${DEFAULT_USER}

# Workers (optional)
#
# You can add multiple workers using this pattern:
#   WORKER1_HOST=...  WORKER1_IP=...  WORKER1_USER=...
#   WORKER2_HOST=...  WORKER2_IP=...  WORKER2_USER=...
# etc.
#
# Provide either HOST or IP per worker. If both are set, HOST wins.
WORKER1_HOST=
WORKER1_IP=
WORKER1_USER=${DEFAULT_USER}

# netcup-kube-tunnel defaults
# (If you set TUNNEL_HOST/TUNNEL_USER explicitly, they override MGMT_*.)
TUNNEL_HOST=${MGMT_HOST}
TUNNEL_USER=${MGMT_USER}
TUNNEL_LOCAL_PORT=6443
TUNNEL_REMOTE_HOST=127.0.0.1
TUNNEL_REMOTE_PORT=6443


